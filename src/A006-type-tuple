/* eslint-disable no-undef */
// tuple, tamanho fixo e tipos especificos

let x;
if (typeof x === 'undefined') x = 20;
console.log(x * 2);
export function createPerson(
  firstName: string,
  lastName?: string,
): {
  firstName: string,
  lastName?: string,
} {
  return {
    firstName,
    lastName,
  };
}
